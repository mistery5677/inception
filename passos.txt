1. Install debian last version
2. Inside debian do the commands:

2.1 -> su -
2.2 -> usermod -a -G sudo vboxuser
2.3 -> sudo apt update
2.4 -> sudo apt install docker.io -y
2.5 -> sudo apt install docker-compose -y
2.6 -> sudo usermod -aG docker vboxuser
2.7 -> reboot
2.8 -> sudo apt install make git curl -y
2.9 -> Create the folders 
        mkdir -p /home/miafonso/data/wordpress
        mkdir -p /home/miafonso/data/mariadb
2.10 -> sudo apt install mariadb-server
2.11 
